#include	<md.h>
#include	<system.h>

/* calculate the electric forces on all the atoms due to a constant electric
 * field */

double ElecPot(pos, force)
	tripd	*pos;
	tripd	*force;
{
	return(0.0);
}
